program financeiro;

uses
  System.StartUpCopy,
  FMX.Forms,
  ufrm_financeiro in 'ufrm_financeiro.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
